apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: oasa-portal-config
  namespace: oasa-telemat
resources:
  - deployment.yaml
  - service.yaml
generatorOptions:
  disableNameSuffixHash: true # use a static name
