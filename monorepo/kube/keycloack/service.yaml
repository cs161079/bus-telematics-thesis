apiVersion: v1
kind: Service
metadata:
  name: keycloak-svc
  namespace: oasa-telemat
spec:
  selector:
    app: keycloak
  ports:
    - name: http-svc
      protocol: TCP
      port: 8081
      targetPort: 8080