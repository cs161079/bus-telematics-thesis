apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: oasa-cronjob-config
  namespace: oasa-telemat
resources:
  - shared-pvc.yaml
  - pvc.yaml
  - config-map.yaml
  - cronjob.yaml
generatorOptions:
  disableNameSuffixHash: true