apiVersion: apps/v1
kind: Deployment
metadata:
  name: otp-deployment
  namespace: oasa-telemat
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: otp-server
  template:
    metadata:
      labels:
        app: otp-server
    spec:
      containers:
      - name: otp-container
        image: cs161079uniwa/otp-server:0.0.1
        imagePullPolicy: IfNotPresent
        env:
          - name: TZ
            value: "Europe/Athens"
          - name: OTP_JAVA_XMX
            value: "4G"
          - name: ROUTER_DIR
            value: "/data/routers/default"
        ports:
          - name: http
            containerPort: 8081
        startupProbe:
          httpGet:
            path: /otp/actuators/health
            port: http
          failureThreshold: 30
          periodSeconds: 10
        readinessProbe:
          httpGet: { path: /otp/actuators/health, port: http }
          failureThreshold: 3
          periodSeconds: 10
          timeoutSeconds: 2
        resources:
          requests:
            cpu: "500m"
            memory: "4Gi"
          limits:
            cpu: "2"
            memory: "6Gi"
        volumeMounts:
        - name: data
          mountPath: /data  
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: otp-shared-pvc